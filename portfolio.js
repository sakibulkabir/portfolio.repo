var typed = new Typed(".typing", {
    strings:["","Web Developer", "web Desinger", "Frontend Developer"],
    typeSpeed:100,
    BackSpeed:60,
    loop:true
})

var typed = new Typed(".name", {
    strings:["","SAKIBUL", "KABIR", "RISAT"],
    typeSpeed:100,
    BackSpeed:5,
    loop:true
})